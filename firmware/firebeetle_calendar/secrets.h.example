#pragma once

// Copy this file to secrets.h and fill in your values.
// secrets.h is gitignored and never committed.
const char* WIFI_SSID   = "";
const char* WIFI_PASS   = "";
const char* SERVER_HOST = "";  // DHCP-reserved IP of the calendar server
